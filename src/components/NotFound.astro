---
interface Props {
  message?: string;
}

const { message = "La página que buscas no existe." } = Astro.props;
---

<section class="min-h-screen flex flex-col items-center justify-center bg-gray-100 px-6 py-16">
    <div class="max-w-4xl mx-auto text-center">
        <img src="/tadeo.avif" alt="Página no encontrada" class="w-64 md:w-80 mx-auto mb-6" loading="lazy" />
        
        <h2 class="text-4xl md:text-5xl font-bold text-red-600 mb-4">
            ¡Oops! Página no encontrada
        </h2>

        <p class="text-lg md:text-xl text-gray-700 mb-4">
            {message}
        </p>

        <p class="text-gray-600 mb-6">
            Parece que te has perdido en la selva amazónica. No te preocupes, te ayudaremos a volver.
        </p>

        <a href="/" class="inline-block px-6 py-3 bg-green-950 text-white text-lg font-medium rounded-lg hover:bg-orange-700 transition transform hover:scale-105">
            Volver al inicio
        </a>
    </div>
</section>